import { StyleSheet } from "react-native"
export const styles = StyleSheet.create({
  tabBar: { position: "absolute", borderTopWidth: 0 },
  scene: { backgroundColor: "transparent" },
  label: { fontSize: 10, textAlign: "center", width: "100%", height: 24, lineHeight: 12 },
})
